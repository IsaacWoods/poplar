[package]
name = "std"
version = "0.1.0"
authors = ["Isaac Woods"]
edition = "2024"

[dependencies]
poplar = { path = "../poplar" }
linked_list_allocator = "0.10.5"
spinning_top = "0.3.0"

[features]
ddk = ["poplar/ddk"]
async = ["poplar/async"]

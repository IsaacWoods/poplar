[package]
name = "pebble_util"
version = "0.1.0"
authors = ["Isaac Woods"]
edition = "2018"

[dependencies]
cfg-if = "0.1"
bit_field = "0.10"
num-traits = { version = "0.2", default-features = false }

[features]
has_alloc = []

[package]
name = "pci_bus"
version = "0.1.0"
authors = ["Isaac Woods"]
edition = "2021"

[dependencies]
std = { path = "../../lib/std" }
log = "0.4"
pci_types = { path = "../../lib/pci_types" }
platform_bus = { path = "../platform_bus" }
ptah = { path = "../../lib/ptah" }
